task dockerCompose(type: Exec, dependsOn: 'installDist') {
    executable = 'docker-compose'
    args = ['up',
//            "-f", "$rootDir/docker-compose.yml",
//            "-p", project.name.toLowerCase(),
            '-d',
            '--build',
            '--force-recreate']
}
